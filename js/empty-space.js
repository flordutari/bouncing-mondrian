'use-strict'

'use-strict'

class EmptySpace{
    constructor(canvas, x, y){
        this.canvas = canvas;
        this.ctx = this.canvas.getContext('2d');
        this.size = 20;
        this.x = x;
        this.y = y;
    };

    draw(){
        
    };

    checkIfBall(){
        
    }
   
};